# Contributing
1. Install packages
    ```shell
    pip install -r requirements.dev.txt
    ```

2. Install pre-commit config
    ```shell
    pre-commit install
    ```

3. Move to dev branch
    ```shell
    git checkout dev
    ```